tap "homebrew/bundle"

# Editor
brew "neovim"

# Core tooling used by config / plugins
brew "git"
brew "fzf"               # junegunn/fzf, fzf.vim
brew "ripgrep"           # fast grep (ack.vim fallback / general use)
brew "ack"               # mileszs/ack.vim
brew "universal-ctags"   # szw/vim-tags

# Language runtimes for vim-test / ALE / projectionist
brew "node"              # jest, typescript, eslint
brew "ruby"              # rspec, vim-rails, vim-bundler

# Font
cask "font-inconsolata"
